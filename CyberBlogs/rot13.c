void idea(char *p1)
size_t len;
uint counter;
char storage(12);

rotator =0;
while(true) {
    len = strlen(p1)
    if (len <= rotator)
}

Wish I took better notes while reading this:
You can work through bitfields via the bitfield editor 
Full support for bitfield references will be in a future release.
